C++ 之 mutable 关键词
====================

C++ 中有两个关键词，mutable 与 volatile，只从词义上，是很类似的：
mutable：不稳定的，易变的
volatile：不稳定的，反覆无常的
不过它们的用法很不一样。

mutable 用于保证，即使类被定义成常对象，那么被修饰的成员也是可以被修改的。这样，当想要修改相应变量的时候，是不用做“去常”的类型转变的。
volatile 则与多线程编程有关。
