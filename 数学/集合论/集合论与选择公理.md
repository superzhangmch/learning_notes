选择公理(Axiom of Choice, AC), 本身是集合论的内容. 其他地方用到它, 是因为集合论是数学基础, 无处不在而已. 作为公理化的集合论的内容, 有它没它决定了什么可以算作一个集合的集合的定义. 

## ZF 公理体系

### (1). 公理有哪些

(下面axioms, 不知道全不全)
- **外延公理** 两个集合相等 ⇔ 元素完全相同
- **空集公理** 存在空集 ∅
  - 为什么需要： 集合论要有个起点。光有构造规则，没有初始对象，什么都造不出来。空集是一切集合的"原材料零件"。结合外延公理，空集是唯一的，记作 ∅。
- **配对公理** {a, b} 是集合
  - 它是从已有集合生成新集合的最基本手段之一
- **并集公理** 给一个集合的集合 ℱ，可以把 ℱ 里所有集合的元素合并成一个新集合 ⋃ℱ
- **幂集公理** 给任意集合 A，它的所有子集构成一个新集合 𝒫(A)
  - 为什么需要： 没有它，子集的"全体"只是一个说法，不是合法的集合，不能作为对象使用。
- **无穷公理** 存在无穷集合.如果没它, 你可以用其他公理 ∅ => {∅} => {{∅}} => {∅, {∅}} …… 一路造下去, 但是因为都是有限步, 造出的集合都是有限集合。不管怎么配对、并集、幂集，每次操作结果都是有限的。你永远造不出一个无穷集合。没有 ℕ，没有 ℝ，数学地基塌了。无穷公理就是专门声明：存在一个无穷集合。
  - 等于说他声明了极限集合的存在.
- **分离公理模式** 给一个已有集合 A 和一个性质 φ，可以从 A 里筛出满足 φ 的元素，构成子集: B = {x ∈ A | φ(x)}
  - 为什么需要 A：朴素集合论允许 {x|φ(x)}——任意性质直接定义集合。Russell 悖论证明这是灾难性的：令 φ(x) := x∉x，得到矛盾。分离公理的修复是：必须在一个已有集合内部筛选，不能凭空造集合。
- **替换公理模式** 如果 φ(x,y) 在集合 A 上定义了一个函数（每个 x ∈ A 对应唯一的 y），那么这个函数的像是一个集合: B = {y | ∃ x ∈ A, φ(x, y)}
  - 为什么需要： 分离公理只能造子集，像不能比原集合大。替换公理突破了这个限制，允许把集合"映射"到一个全新的集合上。很多序数、基数的构造离不开它。分离是"筛"，替换是"换"。
- **正则公理** 对任意非空集合 A，至少存在一个元素 x ∈ A，使得 x ∩ A = ∅。至少一个，可以有更多。
  - 在 ZFC 里，所有东西都是集合。元素也是集合，没有"纯粹的元素"这种东西。所以说 x ∈ A，x 是 A 的元素，同时 x 本身也是一个集合，可以有自己的元素，可以和别的集合求交集。
  - 它是来排除病态情况的: (1). 集合包含自身 A = {A}, A 是一个集合，但它的唯一元素是它自己。(2). 循环包含 A ∈ B ∈ A (3). 无穷下降链 ⋯ ∈ A₃ ∈ A₂ ∈ A₁ ∈ A₀, ∈ 关系一直往下，没有底。

数学研究的对象, 都是以某种编码的方式, 通过以上公理呈现的, 比如:
- 自然数 N:
  - von Neumann 编码: 0 := ∅; 1 := {∅} = {0}; 2 := {∅, {∅}} = {0, 1}; 3 := {∅, {∅}, {∅, {∅}}} = {0, 1, 2}
    - 正是由无穷公理, 保证了由此方法推演出的 N 存在
  - Zermelo 编码：0 := ∅, 1 := {∅}, 2 := {{∅}}, 3 := {{{∅}}}
- 有了N, 有理数Q、实数R, 都可以编码出

### (2). 哪些不是集合

ZFC 构造出来的集合，构成一个宇宙，叫做 V = $⋃_{α} V_α$, ZFC 声明的集合，都在这个 V 里面。但 V 本身不是集合. (如果它是集合，就可以对它用分离公理，造出：R = {x ∈ V | x ∉ x}, 这就是 Russell 悖论，矛盾).

不在 V 里的: 真类（proper class）：

| 对象 | 原因 |
|------|------|
| 所有集合的全体 V | 太大，装不进集合 |
| 所有群的全体 | 太大 |
| 所有序数的全体 | Burali-Forti 悖论 |

这些东西"存在"，但不是集合，在 V 之外。

但所有"正常数学"用到的对象：ℕ, ℤ, ℚ, ℝ, ℂ, 函数、多项式、拓扑空间、流形、群、环、范畴……, 都在 V 里(适当编码后)。

--

## 加上 AC => ZFC 公理体系

### (1). 什么是 AC

**选择公理（AC）** 对任意一族非空集合，**存在一个选择函数** f，使得 f 从每个集合里恰好取出一个元素。这个 f 的值域 {f(A) | A ∈ ℱ} 就是那个"选出来的集合"。

**ZFC = ZF + AC**，这是当代数学使用的标准集合论体系。
 
Gödel（1938）与 Cohen（1963）共同证明：

| 结论 | 含义 |
|------|------|
| **AC 与 ZF 相容** | ZF 无法证明 AC 为假 |
| **AC 独立于 ZF** | ZF 无法证明 AC 为真 |

> AC 是真正意义上的**公理**：加不加它，ZF 都是自洽的，但数学世界的样貌截然不同。

给定无限族非空集合 ${Aᵢ} _ {i ∈ I}$，AC 说：选择函数存在, $f: I → ⋃_{i ∈ I} Aᵢ, f(i) ∈ Aᵢ$. 这个 f 的值域：{f(i) | i ∈ I} 就是"选出来的集合"。

AC 的本质, 在于以一定方式, 扩充了什么样的集合存在: 无限族非空集合的"同时选取"是合法的. 它根本上是关于怎么从一个集合族, 获得另一额集合, 函数 f 只是描述"怎么取"的工具，不是重点。

没有 AC 之前，{f(i)|i∈I} 这个对象：你能描述它, 但不能保证它是合法的集合. ZF 的其他公理推不出它存在. AC 加进来之后：它变成合法的集合了. 这就是 AC 的意义.

集合的定义本身（外延公理）没有因为 AC 而变, 但"哪些对象是集合"这个范围扩大了.

### (2). f 的存在好像是显然的啊, 为啥能有关于它是否存在的争论 (每个set 里任选一个, 这不很显然!)

首先要说, 如果是有限个集合, 每个集合都有限, 有限步遍历选取就行, 不会有任何问题. 问题出在无限个集合的时候.

A₁, A₂, A₃, … 无穷多个, 你说"每个里面取一个"，但：你用什么规则取？
- 如果每个 Aᵢ 都有具体结构，比如都是实数区间，可以说"取最小值"，这没问题，不需要AC。
- 如果 Aᵢ 是任意的非空集合，没有任何额外结构，没有大小，没有顺序，什么都没有：你没有任何规则可以描述"怎么取"。

例子: 
- 不需要AC: 平面上所有直线上分别取一个点构成的集合. (平面上所有直线, 是一个集合: 每条直线可以用斜率和截距来表示：(k, b) ∈ ℝ² , 再加上竖直线单独处理）), 直线上没有天然的第一个点, 或确定位置的点, 这个点集好像需要用 AC, 可是如果按: 都取和x轴的交点, 如果没有则取和y轴交点, 则不用 AC.
- 需要AC: ℝ 的所有(或可列个)非空子集，每个里面取一个元素，凑成一个集合。

注意: 无限族集合, 每个集合有限, 也可能是需要选择公理的. 如果没法统一的有限而具体的规则来指定.

### (3). 争议
AC：只说存在，不说是哪个. ∃f, f(i) ∈ Aᵢ, 只说选择函数存在，但：不说 f 长什么样, 不说从每个 Aᵢ 里选的是哪个元素. 可能有很多个合法的 f，AC 不指定是哪一个. 这就是AC 被争议的根源. 构造主义数学家（比如 Brouwer）说：你说存在，但你造不出来，你给不出来，这算什么存在？

### (4). 什么时候真正需要用 AC

集合数量: 有限/可列无限/不可列; 每个集合: 有限/可列无限/不可列. 组合后一共九种情况: 

| 族的大小 \ 每个集合 | 有限 | 可列无限 | 不可列无限 |
|-------------------|------|---------|-----------|
| **有限族** | ✗ | ✗ | ✗ |
| **可列无限族** | ✓ | ✓ | ✓ |
| **不可列无限族** | ✓ | ✓ | ✓ |

(note: 以上需要的组合, 特殊case 可能不需要) 

特别地, 对于可列无穷族集合, 有弱化版本的 AC: 可数选择公理 (Axiom of Countable Choice, CC), 对可列族非空集合，选择函数存在. 它比 AC 弱，只声明可列族的情况。可数选择公理, 直觉就是, 对于任一个集合, 你都能亲自给它做一个真实选择, 只是永远有不确定的一些sets, 没做选择.

不同强度的 AC:
- CC：可数选择公理 (Axiom of Countable Choice), 对可列族非空集合，选择函数存在
  - 若 {Aₙ}_{n∈ℕ} 是可数多个非空集合, Aₙ没限制可以不可数，则存在选择函数：f: ℕ → ⋃ₙAₙ, f(n)∈Aₙ
  - 有可数个盒子，每个非空, 可以同时从每个盒子里各取一个元素, 各个选择之间相互独立，无依赖关系
- DC：依赖选择公理 (Axiom of Dependent Choice), 如果每步选择依赖上一步的结果，这样的选择序列存在
  - 若 R 是集合 X 上的一个全关系（即 ∀x∈X, ∃y∈X, xRy），则存在序列 x₀, x₁, x₂, … 使得：xₙRxₙ₊₁ ∀n∈ℕ
    - 实际应用中, 往往根据关系 R 选下一个xₙ₊₁很困难, 而只是能证明它存在, 于是经过链式扩展, 说无穷序列存在.
  - 例子:
    - König 引理: 一棵树T每个分叉都是有限个子节点, 一共有无穷个节点, 那么T有沿着子节点的无穷path存在. 证明在于: 每次沿着有无穷子孙节点的子节点推进即可. 但是, 你没法真正知道哪个子节点有无穷子孙节点. 所以实际上不太有可操作性. 
    - Baire 纲定理：构造稠密集中的序列，每个点依赖前一个点的选取/完备度量空间中 Cauchy 列的构造/实分析中大量"逐步逼近"论证
- 强弱关系: CC  ⊂  DC  ⊂  AC

关于AC的争议, 一般大家对于 DC/CC, 更能接受.

### (5). 到底选择是否存在

ZF + ¬AC 与 ZF + AC 都是自洽的. 在ZF框架内, AC是否成立, 永远无法判断. 

----

## 依赖AC的重要定理

### (1). 与AC完全等价的（最强依赖）

这些定理和AC互相能推出对方，本质上就是AC的不同表述：

| 定理 | 领域 | 内容 |
|------|------|------|
| **良序定理** | 序理论 | 任意集合都可以被良序化 |
| **Zorn引理** | 序理论 | 有上界的偏序集有极大元 |
| **Tychonoff定理** | 拓扑 | 任意紧空间的乘积是紧的 |
| **向量空间有基** | 线性代数 | 任意向量空间都有Hamel基 |
| **极大理想存在** | 环论 | 任意非零环都有极大理想 |

### (2). 分析学

| 定理 | 内容 | 依赖强度 |
|------|------|---------|
| **实数的可列并** | 可列个可列集的并还是可列的 | CC够用 |
| **Hahn-Banach定理** | 线性泛函可以延拓 | 需要AC |
| **Baire纲定理** | 完备度量空间不是第一纲的 | DC够用 |
| **每个Hilbert空间有正交基** | 无限维Hilbert空间有基 | 需要AC(L^2这种良好定义的, 不需要AC) |
| **Banach-Tarski悖论** | 球可以分解重组成两个等大的球 | 需要AC |
| **Vitali不可测集** | 存在不可测集 | 需要AC |

### (3). 代数学

| 定理 | 内容 | 依赖强度 |
|------|------|---------|
| **向量空间有Hamel基** | 任意向量空间有Hamel基 | 等价于AC |
| **极大理想存在** | 任意非零环有极大理想 | 等价于AC |
| **代数闭包存在** | 任意域有代数闭包 | 需要AC |
| **每个向量空间的Hamel基等势** | 基的势唯一确定 | 需要AC |
| **群有极大子群** | 无限群有极大正规子群 | 需要AC |

### (4). 拓扑学

| 定理 | 内容 | 依赖强度 |
|------|------|---------|
| **Tychonoff定理** | 紧空间乘积是紧的 | 等价于AC |
| **Stone-Čech紧化** | 任意完全正则空间有紧化 | 需要AC |
| **每个连通图有生成树** | 无限图有生成树 | 需要AC |

### (5). 集合论本身

| 结论 | 内容 | 依赖强度 |
|------|------|---------|
| **基数可比较** | 任意两个基数可以比大小 | 等价于AC |
| **可列个可列集的并可列** | | CC够用 |
| **ℵ运算正常** | 基数算术行为良好 | 需要AC |
| **每个无穷集包含可列子集** | | CC够用 |

### (6). 最反直觉的结论：Banach-Tarski 悖论

一个三维实心球，可以分解成有限块，重新拼装成**两个**和原来等大的球。
- 不违反物理  →  因为分解出来的"块"不是可测集, 根本不能赋予体积
- 违反直觉    →  因为我们默认分解出来的东西有体积
 
### (7). 依赖强度总结

- 等价于AC（最强）:良序定理，Zorn引理, Tychonoff，向量空间有基，极大理想
- 需要完整AC: Hahn-Banach，代数闭包，Banach-Tarski
- DC够用（中等）: Baire纲定理
- CC够用（最弱）: 可列集的并，每个无穷集有可列子集

AC影响的不是数学的边角，而是分析、代数、拓扑的核心定理。拿掉AC，现代数学大面积塌陷——这就是为什么数学家捏着鼻子也要接受它。

### (8). 上述定理稍微解释

- 良序定理: 任意集合都可以被良序化。良序定理说：ℝ 也可以被良序化，只是你写不出来那个顺序长什么样。
- Zorn引理: 偏序集里，如果每条链都有上界，则整个集合有极大元。
  - 偏序集:元素之间有大小关系，但不是所有元素都能比较。
  - 链: 偏序集里能互相比较的一串元素。
  - 数学里用Zorn引理，往往是要证明"某个最大的东西存在"，比如极大理想、极大线性无关组。
- Baire纲定理: 完备度量空间不能写成可列个无处稠密闭集的并。依赖DC
  - 完备空间是"胖"的，不能被可列个"瘦"集合覆盖。
  - ℝ 是完备的，所以不是第一纲的——这排除了很多病态情况。

- 向量空间有基: 任意向量空间都有Hamel基。
  - Hamel基：一组线性无关的向量，使得空间里每个向量都能唯一写成它们的有限线性组合。
- 线性空间基(Hamel basis)的势唯一: 同一个向量空间的任意两组基，元素个数相同。
  - 无限维的情况，需要AC才能保证两组基之间存在双射。
  - 没有这个定理，"维数"这个概念在无限维情况下就不 well-defined 了。

- Hilbert空间有正交基: 任意Hilbert空间都有正交规范基。
  - 一般可分的需要 CC. 对于: L², Gram-Schmidt 正交化可以找到basis, CC 都不用. 量子力学的数学基础建立在L2上. 
  - 任意Hilbert空间 → 可能不可分 → 需要完整AC
- Hahn-Banach定理: 定义在子空间上的有界线性泛函，可以延拓到整个空间，且不增加范数。
  - 在局部定义好的"测量方式"，可以推广到全局。泛函分析的基础定理，没有它，对偶空间理论大面积失效。

- Banach-Tarski悖论: 三维实心球可以分解成有限块，重新拼装成两个等大的球。
  - 分解出来的"块", 不是普通意义上的块, 是无法赋予体积的病态集合
  - AC造出来的，无法构造，无法想象
- Vitali不可测集: 最经典的不可测集构造：在 [0,1] 上定义等价关系：x ∼ y ⇔ x - y ∈ ℚ, 把 [0,1] 分成等价类，每个类里的数相差一个有理数。然后从每个等价类里取一个代表元，凑成集合 V。
  - V 就是Vitali集，可以证明它不可测——无法赋予合理的长度。

- 代数闭包存在: 任意域都有代数闭包。
  - 代数闭包：把一个域扩充，使得所有多项式方程都有根。ℝ 的代数闭包是 ℂ，因为 x²+1=0 在 ℝ 里无解，在 ℂ 里有。
  - 任意域都能做这个扩充，但一般情况需要AC（用Zorn引理）。
- Ring 的 极大理想存在
  - 任意ring 需要 AC. 对于多项式ring比如 k[x₁,…,xₙ] (k是field), 以及 ℤ[x], 他们的极大理想可以显式构造, 不需要 AC
- Stone-Čech紧化: 任意完全正则空间都有紧化。
  - 最简单的紧化：ℝ 加上 ±∞ 变成 [-∞, +∞]，紧了。
  - Stone-Čech紧化是最大的紧化，保留了所有有界连续函数的信息。
- 无限图有生成树. 任意连通图都有生成树。
  - 生成树：图的一个子图，包含所有顶点，连通，无环。
  - 无限图需要AC，因为要同时处理无穷多个顶点的选取。
- 基数可比较: 任意两个基数，要么相等，要么一个严格大于另一个。
  - 没有AC的世界里：可能存在两个集合，互相都不能单射到对方里面——大小无法比较。
  - 有了AC：任意两个集合都可以比较大小(比较元素多少)
- 可列个可列集的并集也可列: A₁, A₂, A₃, … 每个都可列，则 ⋃Aᵢ 也可列。
  - 只需要CC，不需要完整AC。
  - 但没有CC，这个也证不了——你需要从每个Aᵢ里选一个枚举方式(也就是, 每个A_i说它可列, 但是你并不知道怎么列, 所以需要一种方式, 保证能先选出一个, 然后从余下的里面选出一个. 不过一般一说可列, 就暗含着有下标来逐个罗列)

---

## AC 和实践的关系

上面列出的定理, 有实践作用、和现实有关系的, 有这么几个:
- 量子力学用到 Hilbert 空间理论. 但是只用到 L2(L2 和 AC 无关)
- Hahn-Banach: 保证对偶空间足够丰富(对偶空间丰富了，才能建立对偶问题; 对偶问题才有意义; 优化的对偶理论才成立)
  - 单个元素张成的子空间上可以有非平凡泛函, 于是可以用hahn-banach定理 延拓到全空间, 从而全空间有非平凡泛函
  - 但是优化理论在无穷维的时候是 L2
- Baire纲定理. 依赖 DC. 它推出三个:
  - 开映射定理 → 微分方程适定性
  - 闭图像定理 → 微分算子有界性
  - 一致有界原理 → 数值方法稳定性

用于现实世界的定理, 真正依赖 AC 的, 这么看好像没有. 话说回来, 如果必须 AC 才能成立, 那么说明现实世界和 AC 更兼容. 那样的话, 可能AC 没那么大争议了.

另外, AC 和 ZF 独立, 这意味着ZF中的命题的真假, 在有了 AC 之后真假性不变. 所以即使你不接受 AC, 也不用怕出现"AC导出的真命题在 ZF 中为假". 因此大家才放心用它. 其中一些和 AC 等价, 这完全是 AC “空想”出的(典型良序定理), 而大概有一些, 即使没 AC 本也在 ZF 中成立, 这些则算是用 AC 做跳板, 方便证明而已.

-- 

## AC <=> 良序定理 <=> Zorn Lemma

### (1). AC => 良序定理

为啥任意集合都能被良序(有最小元素开始的一种可以逐个往下数的一种序)

- 对集合S, 幂集 P(S), 存在. 根据AC, 有选择函数 f: P(S) -> S. 也就是可以每个子集选一个元素构成一个集合.
- 用超限递归((这是做定义, 做构造, 不是超限归纳做证明)造序列: 
  - 首元: 任意选 S 中一元 x₀, 乃良序之首元素. 选一个: x₁ = f(S \ {x₀})
  - 选下一个: 对已经选出的序列 {x₀, x₁, ..., xₙ}, 选 xₙ₊₁ = f(S \ {x₀, x₁, ..., xₙ}). 对于 ω+1 类似操作
  - 极限选择: 序号 ω=limit of {1, 2, ..., n, ∞}, x_ω = f(S \ {x₀, x₁, ..., xₙ, ..}). 其他极限步都类似操作
- 如是构造的序, 就是良序.

注意: 这有两个要点: 一个是即使是R这样的集合, 有公理保证幂集存在; 二是选择公理. 对于 R, 有良序, 但是不可具体构造. 我觉得神奇发生在, 它的幂集存在.

### (2). 良序定理 => AC

对集合族 {Iᵢ}ᵢ, 在它们的并集 ⋃Iᵢ有良序. 于是可以按序号罗列. 根据良序定义, 每个 Iᵢ都有这个序的最小元. 于是 f(Iᵢ) = min(Iᵢ). 

### (3). 良序定理 => Zorn Lemma

Zorn Lemma: 一个偏序集S(有元素不可比较的序), 如果每个子 chain 都有上界, 则S有极大元. 
- 说明S有至少一个极大元. 极大元不是最大元.
- 子 chain 有上界, 不是说 S 的最大 chain 有上界(否则不用证明了), 而是任意一个最大chain 的一部分都有界.
- 这个子chain 可能是有限长, 也可能无限长. 无限长也可能有界. 比如 chain {[1, 2/(1+1/n)], n=1, 2, ..} ⋃ [1,2) (按集合包含关系的序).
- 它其实是说: 每个 sub-chain, 一直往前走, 都可以到头. 每个最长chain, 都是有具体的 end 端点的.
 
证明: 

S 上存在良序, 于是可以构造一条chain, 这条chain 有最大元(全局的极大元). 怎么构造(用超限**递归**法): 要点是逐步构建一条终究会到头并stop的偏序chain——尽头就是最大元. 构造的时候, 每次选一个偏序上的更大元素, 怎么选, 则是靠良序来指导. 

(i). 构造

利用超限递归构造严格递增链 $(x_α)$。在阶段 α，假设已构造 $C_α$ = {xᵦ : β<α 按偏序}, 考虑集合 $A_α$ = {x∈S : 偏序上 ∀β<α, xᵦ<x} = 当前chain的偏序上界所称的集合 —— 当前chain的前沿是α, 要给它选下一个, 于是构造chain的上界集合(由Zorn 条件, 上界是存在的), 这个集合在良序下一定有良序最小元, 于是可以选择这个特殊的上界最为下一个选出的元素:
- 若 $A_α ≠ ∅$，则利用良序的性质(良序集的任何子集有最小元)有, $x_α = min_良 A_α$.
- 若 $A_α = ∅$，则停止构造。

此定义同时适用于后继步与极限步，无需分别处理。这样选出的chain是偏序排好序的(良序上则不一定). 出了良序、偏序, 这里还有第三个序: 构造序. 上面构造中的 $x_α$ 中的 α 就是构造序. 它其实就是序数列 0, 1, 2, …, ω, ω+1, … 这个序，这个顺序本身也是良序(note: 整个集合论的世界, 只有唯一的一个序数列)。

(ii). 必然停止

以上构造必然结束, 是因为否则的话, 永不停止就能跑完 0, 1, 2, …, ω, ω+1, … 了, 并和原集合 S 有一一对应. 而序数全体不是集合, |S| 只是对应到序数全体这个序列的某个位置的. 矛盾.
- 正式地说: 若构造永不停止，则映射 α ↦ x_α 是所有序数到S的单射，从而所有序数构成的类可以嵌入集合S——这违反了Burali-Forti悖论（序数全体不是集合，不能嵌入任何集合）.

必然结束, 只和构造序有关, 和引入的挑选下一个元素的良序无关.

(iii). 一些解释: 关于停止

stop 指的是这一步没有新的元素选到, 就此打住了. 如果是有限 step 就停止, 那么轮不到选择公理. 那么无穷步停止, 是在 ω²+100 这种确切的地方取得最后一个元素(某 limit 后有限step), 还是在 ω² 这种 limit 位置正巧取完? 答案是, 都有可能.

- S = {[1, 2/(1+1/n)], n=1, 2, ..} ⋃ [1,2), 按集合包含关系定义了偏序. 对它用上面的方法做构造, 正是在极限步 ω 的时候, 构造了最后一个元素, 在 ω+1 时stop.
- S = {[1, 2/(1+1/n)], n=1, 2, ..} ⋃ [1,2) ⋃ {[1, 1+2/(1+1/n)], n=1, 2, ..10}, 同样重复那个构造过程, 极限步 ω 的时候, 选出了 [1,2), 又经过10步, 在 ω+10的时候, 选取了最后一个元素. 并在 ω+11的时候, stop.

### (4). Zorn Lemma => AC

目标: 给定一族非空集合 {Aᵢ}_{i∈I}，要构造选择函数 f(i)∈Aᵢ。

证明:

构造偏序集：所有的部分选择函数所成的集合 S = {f : J→⋃Aᵢ | J⊆I, ∀i∈J, f(i)∈Aᵢ}, 也就是只是在子族上可以做选择的函数的集合. 含定义域的包含关系, 可以定义偏序. 

注意:
- 这样的任意 chain 有上界：把这个偏序上所有部分函数拼起来就是上界--按偏序定义, chain 中函数, 定义不矛盾, 可以拼起来得到“上界”选择函数.
- 任意有限集合上的选择函数一定存在, 都不需要 AC. 所以, 这保证了部分选择函数集合是非空的. 任意Aᵢ, 都好覆盖它的部分选择函数.

因为任意 chain 有界, 由 Zorn：存在极大元选择函数 F. 下面说吗这个 F, 所以 AC 需要的选择函数.

若 F 的定义域 ⊊ I，则可以再扩张一个点(任意一个Aᵢ都有部分选择函数存在, 所以肯定可以扩张)，与极大矛盾. 故 F 的定义域 = I，即为所求选择函数.

### (5). 其他

超限归纳、良序、Zorn引理的应用，本质上都是在做逐步扩张的构造——而"扩张"最自然的数学语言就是集合包含关系。集合包含天然是偏序，使得这些工具可以直接作用其上，因此在具体应用中，偏序集几乎总是以"部分解的集合按包含排序"的面目出现。于是涉及到这几者的时候(无论证明他们本身, 还是应用), 常常以集合包含的形式出现.
