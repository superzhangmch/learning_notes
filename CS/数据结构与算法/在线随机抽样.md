水塘抽样（Reservoir Sampling）是一种在线随机抽样算法，用于在数据量未知或无限、只能遍历一次的数据流中，等概率地随机选出固定数量（如100个）样本。

方法很简单：先把前 k 个元素放入样本池；从第 k+1 个元素开始，对第 i 个元素以 k/i 的概率选中，若选中就随机替换样本池中的一个。一直处理到数据结束，池中 **剩下的** 就是均匀随机样本。

特点：单遍扫描、只占 O(k) 内存、保证等概率随机。
